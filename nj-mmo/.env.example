# Server — SQLite database path (relative to repo root or absolute)
NJ_DB_PATH=data/game.db

# Client (build-time) — Colyseus server endpoint the browser connects to
# Override when running with a tunnel (e.g. ngrok) or a remote server
VITE_COLYSEUS_ENDPOINT=http://localhost:2567
